# Core data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=10.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Web framework
shiny>=0.5.0

# Standard library dependencies (included with Python)
# base64, os, gc, warnings, datetime, json, io, sys
